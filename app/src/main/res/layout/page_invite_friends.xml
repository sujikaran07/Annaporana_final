<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#000000"> <!-- Black background -->

    <!-- Horizontal layout for back arrow and "Settings" label -->
    <LinearLayout
        android:layout_width="390dp"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="24dp">

        <!-- Back arrow icon -->
        <ImageView
            android:id="@+id/backArrow"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="Back"
            android:padding="8dp"
            android:src="@drawable/ic_back_arrow"
            app:tint="#FFFFFF" /> <!-- White arrow -->

        <!-- Settings label -->
        <TextView
            android:id="@+id/invite_friends"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Invite Friends"
            android:textColor="#FFFFFF"
            android:textSize="24dp"
            android:layout_marginStart="8dp"/> <!-- Space between arrow and text -->

    </LinearLayout>

    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/ic_profile"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="32dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Name"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        android:textColor="#FFFFFF"
        android:textSize="20sp"/>


    <!-- Container for Recipe Icon and Count -->
    <LinearLayout
        android:id="@+id/recipeContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <!-- Recipe Count Icon -->
        <ImageView
            android:id="@+id/recipeIcon"
            android:layout_width="25dp"
            android:layout_height="24dp"
            android:src="@drawable/image1"
            android:contentDescription="Recipe Icon"
            app:tint="#FFFFFF" />

        <!-- Recipe Count -->
        <TextView
            android:id="@+id/recipeCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="4dp"
            android:text="   0 recipes"
            android:textColor="#FFFFFF"
            android:textSize="20sp" />
    </LinearLayout>



    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Share a link to your profile with friends and family"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="32dp"
        android:textColor="#FFFFFF"
        android:textSize="16sp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/edit_text_background"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="400dp"
        android:padding="12dp">


        <TextView
            android:id="@+id/invite_Text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="                          Invite with Email"
            android:textColor="#FFFFFF"
            android:textSize="20sp"

            android:textAllCaps="false" />
    </LinearLayout>
    <!-- Invite friends via Button with Icon -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/edit_text_background"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="10dp"
        android:padding="12dp">

        <ImageView
            android:id="@+id/shareIcon"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/ic_share_24"

            app:tint="#FFFFFF" />

        <TextView
            android:id="@+id/invite_Via"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="                     Invite friends Via..."
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textAllCaps="false"
            app:tint="#FFFFFF" />
    </LinearLayout>
     </LinearLayout>
